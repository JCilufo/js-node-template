const name = "Joshua Cilufo";

const course = "Web Development 1";

const grade = 100;

const tradString =
	name + ` is taking ` + course + ` and expects to earn ` + grade + `%.`;

const tempLiteral = `${name} is taking ${course} and expects to earn ${grade}%.`;

console.log(tradString, tempLiteral);
